// Other Common Selectors on all types of meeting creation pages

const startMeetingButton = 'role=button[name="Start Meeting"]';
const retrospectiveOption = 'role=img >> nth=1';

export { 
  startMeetingButton, retrospectiveOption

};
